# Empty is suitable only for the default loopback + private Docker network.
# For shared/untrusted networks use a long random token and configure MiyoHub's Bearer Token.
CAPTCHA_API_TOKEN=
ORT_THREADS=2
